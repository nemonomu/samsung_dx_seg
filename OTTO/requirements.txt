beautifulsoup4>=4.12,<5
lxml>=5,<6
pdfplumber>=0.11,<1
# OCR fallback for image-only (scanned) EU energy datasheets (common/ocr.py).
easyocr>=1.7,<2
PyMuPDF>=1.24,<2
# PostgreSQL load (step14 db_save). Required for a real --only ...,db run.
psycopg2-binary>=2.9,<3
# playwright is only needed for the legacy --transport zenrows/local PDP scrapers;
# the default --transport api path does not require it.
playwright>=1.40,<2
