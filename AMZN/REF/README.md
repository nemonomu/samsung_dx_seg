# Amazon.de SEG REF

REF is intentionally left as a category placeholder until the TV crawler is verified.
Reuse `AMZN/common` and add `REF/config.py` plus `REF/run.py` after confirming the TV
listing, detail/review, full output, DB, and email contracts.
